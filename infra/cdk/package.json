{
  "name": "wishlist-cdk",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "cdk": "cdk",
    "synth": "cdk synth",
    "deploy": "cdk deploy --all --profile wishlist-deploy",
    "destroy": "cdk destroy --all --profile wishlist-deploy",
    "diff": "cdk diff --profile wishlist-deploy"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.180.0",
    "constructs": "^10.4.0"
  },
  "devDependencies": {
    "aws-cdk": "^2.180.0",
    "typescript": "^5.7.0"
  }
}
